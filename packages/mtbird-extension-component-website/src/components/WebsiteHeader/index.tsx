import React from 'react';
import manifest from './manifest';

const WebsiteHeader = () => {
  return <div>this is a header 导航栏</div>;
};

WebsiteHeader.manifest = manifest;

export default WebsiteHeader;
