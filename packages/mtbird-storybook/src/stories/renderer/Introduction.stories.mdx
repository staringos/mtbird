import { Meta } from "@storybook/addon-docs";
import LinkTo from "@storybook/addon-links/react";

<Meta title="渲染器/介绍" parameters={{ viewMode: "docs" }} />

# 渲染器介绍

渲染器是星搭用来渲染经过编辑器编辑的页面的 React 组件。传入一个根据我们标准建立的 PageConfig 数据结构后，即可渲染出目标页面。

下一篇：<LinkTo kind="渲染器-嵌入渲染器--page">嵌入渲染器</LinkTo>
